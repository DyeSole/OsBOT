FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

RUN pip install --no-cache-dir --upgrade pip \
    && pip install --no-cache-dir pyarmor==9.2.4

WORKDIR /workspace

ENTRYPOINT ["/bin/bash", "scripts/obfuscate_in_docker.sh"]
